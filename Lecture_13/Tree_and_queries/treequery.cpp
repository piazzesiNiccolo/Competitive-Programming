#include <iostream>
#include <functional>
#include <vector>
#include <set>

#define LEFT(i) (2*i + 1)
#define RIGHT(i) (2*1 + 2)

using namespace std;

int main(){
    int n, m;
    vector<int> colours;
    mul
    
    return 0;

}